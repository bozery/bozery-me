<coast-background aria-hidden="true" transition:persist="coast-background" transition:animate="none">
  <div class="coast-fallback"></div>
  <canvas></canvas>
  <div class="coast-atmosphere"></div>
</coast-background>

<script>import '../scripts/coast-background';</script>

<style>
  coast-background {
    position: fixed;
    inset: 0;
    z-index: -2;
    display: block;
    pointer-events: none;
    overflow: hidden;
    background: #368e92;
  }
  canvas, .coast-fallback, .coast-atmosphere {
    position: absolute;
    inset: 0;
    width: 100%;
    height: 100%;
  }
  canvas { opacity: 0; }
  coast-background[data-rendered] canvas { opacity: 1; }
  .coast-fallback {
    background:
      linear-gradient(146deg, transparent 70%, #cbdccb 74%, #f0edda 74.4%, #cdc6aa 75%, #e6d9bc 81%),
      radial-gradient(ellipse at 74% 60%, #8bccbf, transparent 65%),
      linear-gradient(120deg, #1f626d, #40949a 55%, #8ccdc2);
  }
  .coast-atmosphere {
    background: linear-gradient(90deg, #0e465a20, transparent 48%), linear-gradient(180deg, #e4f8ea06, transparent 68%);
  }
  @media (max-width: 700px) {
    .coast-fallback {
      background:
        linear-gradient(164deg, transparent 68%, #cbdccb 72%, #f0edda 72.4%, #cdc6aa 73%, #e6d9bc 79%),
        linear-gradient(140deg, #286f78, #6eb8af 76%, #a5d6c5);
    }
  }
</style>
